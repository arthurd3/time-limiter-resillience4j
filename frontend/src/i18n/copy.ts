/**
 * Every string on the page, in both languages.
 *
 * `Copy` is derived from the English object, so the Portuguese one cannot compile while a key is
 * missing. Paragraphs are stored whole -- see RichText for the inline vocabulary they may use.
 */

export type Locale = 'en' | 'pt'

const en = {
  meta: { title: 'How @TimeLimiter Works' },

  ui: {
    caller: 'caller',
    worker: 'worker',
    callerSub: 'waiting for a response',
    workerSub: 'doing the work',
    play: 'Play',
    replay: 'Replay',
    inferred: 'inferred',
    outcomeNotes: {
      UPSTREAM: 'the dependency answered in time',
      FALLBACK: 'the deadline fired and the fallback answered',
      ERROR: 'the deadline fired and no fallback resolved',
    },
  },

  hero: {
    eyebrow: 'Resilience4j · TimeLimiter',
    titleA: 'A call with no deadline doesn’t fail slowly.',
    titleB: 'It turns latency into concurrency.',
    p1: 'That trade is the whole problem. Every request still waiting is a thread still held, so when a dependency slows down by 100×, you need 100× the threads to serve the same traffic. You don’t have them. The service stops answering — including for requests that never touched the slow dependency at all.',
    p2: 'A **TimeLimiter** is how you refuse that trade. This page shows what it does, what it deliberately does *not* do, and the two ways it fails silently.',
    nav: {
      hang: 'The hang',
      exhaustion: 'Why it spreads',
      deadline: 'The deadline',
      trap1: 'Trap 1',
      trap2: 'Trap 2',
      live: 'Try it live',
      choosing: 'Choosing a value',
    },
  },

  hang: {
    title: 'First, wait for it',
    p1: 'The demo API behind this page has one endpoint. Ask it for a forecast and it calls a dependency that takes five seconds. No timeout, no fallback — just the call.',
    p2: 'Press the button. It is not broken and there is no trick: the wait below is real, and it is the last one on this page. Everything after it runs on simulated time.',
    idle: 'Request the forecast',
    waiting: 'Waiting…',
    again: 'Request again',
    elapsed: 'elapsed',
    after: 'It worked. That is the uncomfortable part — nothing failed, so nothing alerted. A thread sat blocked for five seconds and then returned a perfectly good answer.',
    p3: 'Five seconds for one request is an annoyance. The next section is about what happens when it is not one request.',
  },

  pool: {
    title: 'Why one slow dependency takes everything down',
    p1: 'A pool of threads serves incoming requests. Each request calls the slow dependency and holds its thread until the call returns. Nothing here is failing — the dependency answers every time, just slowly.',
    p2: 'The arithmetic is unforgiving. A pool of {workers} threads, each held for {hold}, retires **{capacity}** requests per second. Ask for more than that and the queue does not stabilise — it grows for as long as the traffic lasts.',
    poolSize: 'Pool size',
    arrivalRate: 'Arrival rate',
    latency: 'Dependency latency',
    timeout: 'Timeout',
    threads: 'threads',
    reqPerSec: 'req/s',
    offered: 'Offered',
    capacity: 'Capacity (no timeout)',
    verdict: 'Verdict',
    saturated: 'SATURATED',
    keepingUp: 'keeping up',
    saturatedNote: 'arrivals exceed what the pool can retire',
    question: 'Before the answer: add a {timeout} timeout. What happens to the load on the dependency itself?',
    guessDown: 'It drops — fewer calls get through',
    guessSame: 'Unchanged — same traffic either way',
    guessUp: 'It rises — more calls reach it',
    noTimeout: 'No timeout',
    withTimeout: 'With a {timeout} timeout',
    p99: 'p99 latency',
    peakQueue: 'Peak queue',
    queueDepth: 'Queue depth',
    peakWaiting: 'peak {n} waiting',
    lookAtDependency: 'But look at the dependency, not at yourself.',
    inFlightNo: 'Calls in flight upstream — no timeout',
    inFlightYes: 'Calls in flight upstream — with timeout',
    peakConcurrent: 'peak {n} concurrent',
    increased: 'The timeout **increased** the load on the struggling dependency, from {before} concurrent calls to {after}. Without a timeout the saturated pool was acting as an accidental throttle: at most {workers} calls could be in flight, because there were only {workers} threads to make them. Retiring requests faster just means making new calls faster.',
    notSaturated: 'At these settings the pool is not saturated, so the timeout changes little. Push the arrival rate up until the verdict reads SATURATED and compare again.',
    misconception: 'Worth sitting with if that is not what you picked. It is the single most common misconception about timeouts.',
    p3: 'So a timeout protects **the caller**, and only the caller. It bounds what you are willing to wait; it cannot reach across the network and stop work already running. If the dependency is failing rather than merely slow, you need something that stops calling it — which is the argument for pairing a TimeLimiter with a circuit breaker or a bulkhead, not treating it as the whole answer.',
  },

  deadline: {
    title: 'The deadline',
    p1: 'A TimeLimiter is a race between two clocks. The <worker>worker</worker> is trying to produce a value; the <deadline>deadline</deadline> is counting down. Whichever finishes first decides what the <caller>caller</caller> gets.',
    latency: 'Dependency latency',
    timeoutDuration: 'timeout-duration',
    race: 'The two are within {margin} of each other. Real calls do not resolve this cleanly: around the deadline the outcome is a genuine race, and the same request can land either way from one run to the next.',
    p2: 'Two things about the annotation are not optional, and both are easy to get wrong without anything telling you.',
    codeCaption: 'SlowWeatherApiClient.java — the return type and the bean boundary both matter',
    p3: 'The return type has to be a `CompletionStage`. A deadline is only enforceable against a value that is still pending — hand the aspect a value that has already been computed and there is nothing left to time out. This is why wrapping a blocking call in `CompletableFuture.completedFuture(...)` is a silent no-op: the blocking already happened before the aspect ever saw it.',
    p4: 'And the annotation is applied by a proxy, so the call has to arrive from outside the bean. A method calling its own annotated method goes straight down the inside of the object and never touches the proxy — no deadline, no warning, no error. Here that is why the client is a separate bean from the service rather than one class with two methods.',
  },

  fallback: {
    eyebrow: '§4 · Trap 1',
    title: 'The fallback that is never called',
    p1: 'Resilience4j resolves `fallbackMethod` by reflection, and it looks for one exact shape: **the guarded method’s own parameters, plus a trailing exception**. A method with the right name and the wrong signature is not a fallback with a problem — it is not found at all.',
    p2: 'This project shipped with that bug. Flip between the two signatures and watch what the caller gets.',
    captionBad: 'No trailing Throwable — never matched',
    captionGood: 'Guarded method’s parameters, then the exception',
    explainBad: 'A `WARN`. That is the entire signal. The application starts, the endpoint responds, the metrics record a timeout — and every caller gets an error where the fallback was supposed to be. Nothing in the type system could have caught it, because the method name is a string.',
    explainGood: 'With the trailing exception present the method resolves and the caller gets a real answer at the deadline. Prefer the narrowest exception type that makes sense — `TimeoutException` here rather than `Throwable`, which would also quietly swallow genuine upstream failures and report them as a healthy fallback.',
    note: 'The 504 above comes from this project’s exception handler mapping an escaped `TimeoutException` to a gateway timeout. Before that handler existed — the state the project originally shipped in — the same failure surfaced as a bare 500.',
  },

  cancel: {
    eyebrow: '§5 · Trap 2',
    title: 'Cancelling is not interrupting',
    p1: 'The setting is called `cancel-running-future`, and it is on by default. It reads like a promise that the slow work stops. It is not.',
    p2: 'Watch the second lane. At the deadline the <caller>caller</caller> is released and gets its answer — but the <worker>worker</worker> keeps going, hollow, for another {wasted}, and only then produces a value that nobody is waiting for any more.',
    toggle: 'cancel-running-future',
    toggleHint: 'Toggle it. The worker lane does not change — that is the entire point.',
    p3: 'The reason is a detail of the JDK, not of Resilience4j. `CompletableFuture.cancel()` takes a `mayInterruptIfRunning` flag and **ignores it**. Unlike `FutureTask`, a `CompletableFuture` has no thread of its own to interrupt; cancelling only completes the wrapper exceptionally. Whatever is running on the pool runs to its natural end.',
    codeCaption: 'The interrupt branch here is unreachable in practice',
    p4: 'So the honest summary is that a timeout frees **the caller**, never the resource. If you need the work itself to stop, the cancellation has to be cooperative: a flag the task actually checks between steps, or — far more usefully — a real client whose own socket and read timeouts sit below the TimeLimiter’s, so the I/O gives up on its own.',
    p5: 'This is also the sharpest argument for a bulkhead. If the work cannot be stopped, the only remaining lever is to cap how much of it can exist at once.',
  },

  live: {
    title: 'Now against a real server',
    p1: 'Everything above was a model. This section calls the actual Spring Boot app in this repository and times it in your browser with `performance.now()` — so the number below is what you would have waited, not what the server claims.',
    omitParam: 'omit param',
    invalid: '40000 (invalid)',
    checking: 'checking API…',
    reachable: 'API reachable',
    unreachable: 'API not reachable',
    startPrompt: 'Start the API, then press Run:',
    worksWithout: 'Everything above this section works without it.',
    send: 'Send a real request',
    running: 'Running…',
    measured: 'Measured',
    measuredNote: 'browser wall clock, incl. network',
    http: 'HTTP',
    outcome: 'Outcome',
    serverAgrees: 'The server agrees. Its own `resilience4j.timelimiter.calls` counter moved by **{delta}** while you were reading this.',
    timeoutDelta: '+{n} timeout',
    successDelta: '+{n} successful',
    provenance: 'The worker bar is dashed because it is inferred, not observed: it is the delay you asked the server for. A browser cannot see what a thread on the server is doing, and drawing it as though it could would be a lie in a page about honesty with timing.',
    aboveDeadline: ' The measured value sits a little above {deadline} — scheduling and network, not a misconfigured deadline.',
    failed: 'Request {kind}.',
  },

  closing: {
    title: 'Choosing a number',
    p1: 'The hardest part of a timeout is not configuring it. It is picking the value, and “a few seconds” is how most of them get chosen.',
    p2: '**Start from an error budget, not from a round number.** Amazon’s approach is to decide what rate of false timeouts is acceptable — say 0.1% — and then read the corresponding latency percentile off the dependency, p99.9 in that example. The number falls out of data you already have. Two caveats come with it: the method breaks down when the latency distribution is flat, because p99.9 sits almost on top of p50 and there is no headroom to buy; and a timeout often does not cover DNS resolution or the TLS handshake, so the wall-clock wait can exceed the number you configured.',
    p3: '**A deadline far above the mean is not conservative, it is a liability.** Google’s SRE book works the arithmetic: a server handling 50 QPS with a 100-second deadline, where 5% of requests hit the slow path, needs 5,000 threads for that 5% alone. Against 1,000 available threads the result was an 80.4% error rate — caused by the deadline, not by the dependency.',
    p4: '**Deadlines have to shrink as they propagate.** If your caller gives up after two seconds, work you start with a three-second budget is work nobody will ever receive. Pass the remaining budget down rather than configuring each hop independently.',
    orderHeading: 'Where it sits among the others',
    p5: 'Resilience4j applies its decorators in a fixed order regardless of how you write the annotations:',
    orderCaption: 'TimeLimiter sits inside Retry — so the deadline is per attempt, not per call',
    p6: 'That nesting has a consequence worth internalising: a “one second timeout” with three retries and a 300 ms wait between them is a **3.6 second** worst case for the caller. The deadline bounds each attempt; nothing bounds the sum unless you add an outer budget.',
    disagreeHeading: 'Two things practitioners disagree about',
    fallbacks: '**Fallbacks.** Netflix built its tooling around them; Amazon argues against them in most cases, having watched a cache fallback turn a partial outage into a total one in 2001. Their objection is sharp: if the fallback path were reliable enough to carry production traffic, why is it not the primary path? A usable test — does the fallback consume a resource that the original failure has already made scarce? If it retries against the same struggling database, it is an amplifier. If it returns a cached, static or degraded answer that costs nothing, it is a genuine safety net. The one in this demo is the second kind.',
    breakers: '**Circuit breakers.** Resilience4j and Netflix treat them as the flagship pattern. Amazon largely does not use them, preferring token buckets, on the grounds that they add modal behaviour that is hard to test — and Marc Brooker puts it more bluntly: modern systems are built to fail partially, and a circuit breaker converts a partial failure into a complete one. Timeouts and bulkheads are uncontroversial. Circuit breakers are a real trade-off, not a default.',
    sourcesHeading: 'Sources',
    src1: '[Timeouts, retries and backoff with jitter](https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/) — Marc Brooker, AWS Builders’ Library. The percentile method, and why retries are “selfish”.',
    src2: '[Avoiding fallback in distributed systems](https://aws.amazon.com/builders-library/avoiding-fallback-in-distributed-systems/) — Jacob Gabrielson, AWS. The 2001 outage and the case against fallbacks.',
    src3: '[Addressing cascading failures](https://sre.google/sre-book/addressing-cascading-failures/) — Google SRE book. The 5,000-thread worked example.',
    src4: '[Circuit breakers](https://brooker.co.za/blog/2022/02/16/circuit-breakers.html) — Marc Brooker’s argument against them.',
    src5: '[Resilience4j Spring Boot documentation](https://resilience4j.readme.io/docs/getting-started-3) — the decorator order above.',
    src6: 'Michael Nygard, *Release It!* — the origin of the Timeout and Bulkhead patterns. “Integration points are the number-one killer of systems.”',
    footer: 'The code behind every figure on this page lives in [arthurd3/time-limiter-resillience4j](https://github.com/arthurd3/time-limiter-resillience4j). The simulations are pure functions under `frontend/src/sim/`, unit-tested against the behaviour described here.',
  },
}

export type Copy = typeof en

/**
 * Brazilian Portuguese.
 *
 * Terms that Brazilian developers say in English stay in English -- thread, timeout, fallback,
 * bulkhead, circuit breaker. Translating those would make the page harder to read, not easier,
 * and would not match the vocabulary anyone uses at work.
 */
const pt: Copy = {
  meta: { title: 'Como o @TimeLimiter funciona' },

  ui: {
    caller: 'chamador',
    worker: 'worker',
    callerSub: 'esperando a resposta',
    workerSub: 'fazendo o trabalho',
    play: 'Rodar',
    replay: 'Repetir',
    inferred: 'inferido',
    outcomeNotes: {
      UPSTREAM: 'a dependência respondeu a tempo',
      FALLBACK: 'o prazo estourou e o fallback respondeu',
      ERROR: 'o prazo estourou e nenhum fallback foi encontrado',
    },
  },

  hero: {
    eyebrow: 'Resilience4j · TimeLimiter',
    titleA: 'Uma chamada sem prazo não falha devagar.',
    titleB: 'Ela transforma latência em concorrência.',
    p1: 'Essa troca é o problema inteiro. Toda requisição que ainda espera é uma thread ainda presa, então quando uma dependência fica 100× mais lenta, você precisa de 100× mais threads para atender o mesmo tráfego. Você não tem essas threads. O serviço para de responder — inclusive para requisições que nunca encostaram na dependência lenta.',
    p2: 'Um **TimeLimiter** é como você recusa essa troca. Esta página mostra o que ele faz, o que ele deliberadamente *não* faz, e as duas maneiras pelas quais ele falha em silêncio.',
    nav: {
      hang: 'A espera',
      exhaustion: 'Por que se espalha',
      deadline: 'O prazo',
      trap1: 'Armadilha 1',
      trap2: 'Armadilha 2',
      live: 'Testar ao vivo',
      choosing: 'Escolhendo o valor',
    },
  },

  hang: {
    title: 'Primeiro, espere',
    p1: 'A API de demonstração por trás desta página tem um único endpoint. Peça uma previsão e ela chama uma dependência que leva cinco segundos. Sem timeout, sem fallback — só a chamada.',
    p2: 'Aperte o botão. Não está quebrado e não tem truque: a espera abaixo é real, e é a última desta página. Tudo depois dela roda em tempo simulado.',
    idle: 'Pedir a previsão',
    waiting: 'Esperando…',
    again: 'Pedir de novo',
    elapsed: 'decorrido',
    after: 'Funcionou. Essa é a parte incômoda — nada falhou, então nada alertou. Uma thread ficou bloqueada por cinco segundos e depois devolveu uma resposta perfeitamente boa.',
    p3: 'Cinco segundos para uma requisição é um incômodo. A próxima seção é sobre o que acontece quando não é uma requisição só.',
  },

  pool: {
    title: 'Por que uma dependência lenta derruba tudo',
    p1: 'Um pool de threads atende as requisições que chegam. Cada requisição chama a dependência lenta e segura sua thread até a chamada voltar. Nada aqui está falhando — a dependência responde sempre, só que devagar.',
    p2: 'A aritmética não perdoa. Um pool de {workers} threads, cada uma presa por {hold}, dá vazão a **{capacity}** requisições por segundo. Peça mais que isso e a fila não estabiliza — ela cresce enquanto o tráfego durar.',
    poolSize: 'Tamanho do pool',
    arrivalRate: 'Taxa de chegada',
    latency: 'Latência da dependência',
    timeout: 'Timeout',
    threads: 'threads',
    reqPerSec: 'req/s',
    offered: 'Oferecido',
    capacity: 'Capacidade (sem timeout)',
    verdict: 'Veredito',
    saturated: 'SATURADO',
    keepingUp: 'dando conta',
    saturatedNote: 'chegam mais requisições do que o pool consegue liberar',
    question: 'Antes da resposta: adicione um timeout de {timeout}. O que acontece com a carga sobre a própria dependência?',
    guessDown: 'Cai — menos chamadas passam',
    guessSame: 'Não muda — mesmo tráfego de qualquer jeito',
    guessUp: 'Sobe — mais chamadas chegam nela',
    noTimeout: 'Sem timeout',
    withTimeout: 'Com timeout de {timeout}',
    p99: 'latência p99',
    peakQueue: 'Pico da fila',
    queueDepth: 'Tamanho da fila',
    peakWaiting: 'pico de {n} esperando',
    lookAtDependency: 'Mas olhe para a dependência, não para você.',
    inFlightNo: 'Chamadas em voo na dependência — sem timeout',
    inFlightYes: 'Chamadas em voo na dependência — com timeout',
    peakConcurrent: 'pico de {n} simultâneas',
    increased: 'O timeout **aumentou** a carga sobre a dependência que já estava sofrendo: de {before} chamadas simultâneas para {after}. Sem timeout, o pool saturado funcionava como um limitador acidental: no máximo {workers} chamadas podiam estar em voo, porque só existiam {workers} threads para fazê-las. Liberar requisições mais rápido significa apenas fazer chamadas novas mais rápido.',
    notSaturated: 'Com esses valores o pool não está saturado, então o timeout muda pouco. Suba a taxa de chegada até o veredito virar SATURADO e compare de novo.',
    misconception: 'Vale parar e pensar, caso não tenha sido essa a sua escolha. É o mal-entendido mais comum sobre timeouts.',
    p3: 'Ou seja: um timeout protege **quem chama**, e só quem chama. Ele limita quanto você aceita esperar; não atravessa a rede para parar um trabalho que já começou. Se a dependência está falhando, e não apenas lenta, você precisa de algo que pare de chamá-la — que é o argumento para combinar o TimeLimiter com um circuit breaker ou um bulkhead, em vez de tratá-lo como a resposta inteira.',
  },

  deadline: {
    title: 'O prazo',
    p1: 'Um TimeLimiter é uma corrida entre dois relógios. O <worker>worker</worker> tenta produzir um valor; o <deadline>prazo</deadline> corre para trás. Quem terminar primeiro decide o que o <caller>chamador</caller> recebe.',
    latency: 'Latência da dependência',
    timeoutDuration: 'timeout-duration',
    race: 'Os dois estão a {margin} um do outro. Chamadas reais não resolvem isso de forma limpa: perto do prazo o resultado é uma corrida de verdade, e a mesma requisição pode cair para qualquer um dos lados entre uma execução e outra.',
    p2: 'Duas coisas na anotação não são opcionais, e as duas são fáceis de errar sem que nada avise.',
    codeCaption: 'SlowWeatherApiClient.java — o tipo de retorno e a fronteira do bean importam',
    p3: 'O tipo de retorno precisa ser um `CompletionStage`. Um prazo só é aplicável a um valor que ainda está pendente — entregue ao aspecto um valor já calculado e não sobra nada para expirar. É por isso que embrulhar uma chamada bloqueante em `CompletableFuture.completedFuture(...)` não faz nada: o bloqueio já aconteceu antes de o aspecto ver qualquer coisa.',
    p4: 'E a anotação é aplicada por um proxy, então a chamada precisa vir de fora do bean. Um método que chama o próprio método anotado desce direto por dentro do objeto e nunca encosta no proxy — sem prazo, sem aviso, sem erro. É por isso que aqui o client é um bean separado do service, em vez de uma classe com dois métodos.',
  },

  fallback: {
    eyebrow: '§4 · Armadilha 1',
    title: 'O fallback que nunca é chamado',
    p1: 'O Resilience4j resolve o `fallbackMethod` por reflexão, e procura por um formato exato: **os próprios parâmetros do método protegido, mais uma exceção no fim**. Um método com o nome certo e a assinatura errada não é um fallback com problema — ele simplesmente não é encontrado.',
    p2: 'Este projeto foi publicado com esse bug. Alterne entre as duas assinaturas e veja o que o chamador recebe.',
    captionBad: 'Sem Throwable no fim — nunca casa',
    captionGood: 'Parâmetros do método protegido, depois a exceção',
    explainBad: 'Um `WARN`. Esse é o sinal inteiro. A aplicação sobe, o endpoint responde, as métricas registram o timeout — e todo chamador recebe um erro onde deveria estar o fallback. Nada no sistema de tipos poderia ter pegado isso, porque o nome do método é uma string.',
    explainGood: 'Com a exceção no fim, o método é encontrado e o chamador recebe uma resposta de verdade no prazo. Prefira o tipo de exceção mais específico que fizer sentido — `TimeoutException` aqui, em vez de `Throwable`, que também engoliria falhas legítimas da dependência e as reportaria como um fallback saudável.',
    note: 'O 504 acima vem do exception handler deste projeto, que mapeia um `TimeoutException` que escapou para um gateway timeout. Antes desse handler existir — o estado em que o projeto foi publicado originalmente — a mesma falha aparecia como um 500 puro.',
  },

  cancel: {
    eyebrow: '§5 · Armadilha 2',
    title: 'Cancelar não é interromper',
    p1: 'A configuração se chama `cancel-running-future`, e vem ligada por padrão. Ela parece prometer que o trabalho lento para. Não para.',
    p2: 'Olhe a segunda faixa. No prazo, o <caller>chamador</caller> é liberado e recebe sua resposta — mas o <worker>worker</worker> continua, vazado, por mais {wasted}, e só então produz um valor que ninguém está mais esperando.',
    toggle: 'cancel-running-future',
    toggleHint: 'Ligue e desligue. A faixa do worker não muda — esse é exatamente o ponto.',
    p3: 'O motivo é um detalhe da JDK, não do Resilience4j. `CompletableFuture.cancel()` recebe uma flag `mayInterruptIfRunning` e **a ignora**. Diferente de `FutureTask`, um `CompletableFuture` não tem uma thread própria para interromper; cancelar apenas completa o wrapper com exceção. O que estiver rodando no pool roda até o fim natural.',
    codeCaption: 'O ramo de interrupção aqui é inalcançável na prática',
    p4: 'Então o resumo honesto é: um timeout libera **quem chama**, nunca o recurso. Se você precisa que o trabalho em si pare, o cancelamento precisa ser cooperativo: uma flag que a tarefa realmente verifica entre as etapas ou — muito mais útil — um client de verdade cujos timeouts de socket e de leitura fiquem abaixo do TimeLimiter, para que o próprio I/O desista.',
    p5: 'Esse é também o argumento mais forte para um bulkhead. Se o trabalho não pode ser parado, a única alavanca que sobra é limitar quanto dele pode existir ao mesmo tempo.',
  },

  live: {
    title: 'Agora contra um servidor real',
    p1: 'Tudo acima era um modelo. Esta seção chama a aplicação Spring Boot de verdade deste repositório e cronometra no seu navegador com `performance.now()` — então o número abaixo é o que você teria esperado, não o que o servidor afirma.',
    omitParam: 'sem o parâmetro',
    invalid: '40000 (inválido)',
    checking: 'verificando a API…',
    reachable: 'API acessível',
    unreachable: 'API inacessível',
    startPrompt: 'Suba a API e depois aperte o botão:',
    worksWithout: 'Tudo acima desta seção funciona sem ela.',
    send: 'Enviar uma requisição real',
    running: 'Executando…',
    measured: 'Medido',
    measuredNote: 'relógio do navegador, inclui a rede',
    http: 'HTTP',
    outcome: 'Resultado',
    serverAgrees: 'O servidor concorda. O contador `resilience4j.timelimiter.calls` dele mudou em **{delta}** enquanto você lia isto.',
    timeoutDelta: '+{n} timeout',
    successDelta: '+{n} sucesso',
    provenance: 'A barra do worker é tracejada porque ela é inferida, não observada: é o atraso que você pediu ao servidor. Um navegador não consegue ver o que uma thread do servidor está fazendo, e desenhar como se conseguisse seria uma mentira numa página sobre honestidade com tempo.',
    aboveDeadline: ' O valor medido fica um pouco acima de {deadline} — escalonamento e rede, não um prazo mal configurado.',
    failed: 'Requisição {kind}.',
  },

  closing: {
    title: 'Escolhendo um número',
    p1: 'A parte difícil de um timeout não é configurá-lo. É escolher o valor, e “uns segundos aí” é como a maioria deles é escolhida.',
    p2: '**Comece por um orçamento de erro, não por um número redondo.** A abordagem da Amazon é decidir qual taxa de timeouts falsos é aceitável — digamos 0,1% — e então ler o percentil de latência correspondente na dependência, p99,9 nesse exemplo. O número sai de dados que você já tem. Duas ressalvas vêm junto: o método quebra quando a distribuição de latência é plana, porque o p99,9 fica quase em cima do p50 e não há folga para comprar; e um timeout muitas vezes não cobre a resolução de DNS nem o handshake TLS, então a espera real pode passar do número que você configurou.',
    p3: '**Um prazo muito acima da média não é conservador, é um risco.** O livro de SRE do Google faz a conta: um servidor atendendo 50 QPS com um prazo de 100 segundos, onde 5% das requisições caem no caminho lento, precisa de 5.000 threads só para esses 5%. Contra 1.000 threads disponíveis, o resultado foi uma taxa de erro de 80,4% — causada pelo prazo, não pela dependência.',
    p4: '**Prazos precisam encolher conforme se propagam.** Se quem te chamou desiste depois de dois segundos, o trabalho que você começa com um orçamento de três segundos é trabalho que ninguém vai receber. Repasse o orçamento restante em vez de configurar cada salto de forma independente.',
    orderHeading: 'Onde ele fica em relação aos outros',
    p5: 'O Resilience4j aplica seus decorators numa ordem fixa, independentemente de como você escreve as anotações:',
    orderCaption: 'O TimeLimiter fica dentro do Retry — então o prazo é por tentativa, não por chamada',
    p6: 'Esse aninhamento tem uma consequência que vale internalizar: um “timeout de um segundo” com três tentativas e 300 ms de espera entre elas é um pior caso de **3,6 segundos** para quem chamou. O prazo limita cada tentativa; nada limita a soma, a menos que você adicione um orçamento por fora.',
    disagreeHeading: 'Duas coisas em que os praticantes discordam',
    fallbacks: '**Fallbacks.** A Netflix construiu seu ferramental em torno deles; a Amazon argumenta contra eles na maioria dos casos, depois de ver um fallback de cache transformar uma indisponibilidade parcial em total em 2001. A objeção é afiada: se o caminho do fallback fosse confiável o bastante para carregar tráfego de produção, por que ele não é o caminho principal? Um teste que funciona — o fallback consome um recurso que a falha original já deixou escasso? Se ele tenta de novo contra o mesmo banco que está sofrendo, é um amplificador. Se devolve uma resposta em cache, estática ou degradada, que não custa nada, é uma rede de proteção de verdade. O desta demo é do segundo tipo.',
    breakers: '**Circuit breakers.** Resilience4j e Netflix os tratam como o padrão principal. A Amazon em geral não os usa, preferindo token buckets, alegando que eles adicionam um comportamento modal difícil de testar — e Marc Brooker é mais direto: sistemas modernos são feitos para falhar parcialmente, e um circuit breaker converte uma falha parcial em uma falha completa. Timeouts e bulkheads são incontroversos. Circuit breakers são um trade-off de verdade, não um padrão automático.',
    sourcesHeading: 'Fontes',
    src1: '[Timeouts, retries and backoff with jitter](https://aws.amazon.com/builders-library/timeouts-retries-and-backoff-with-jitter/) — Marc Brooker, AWS Builders’ Library. O método do percentil, e por que retries são “egoístas”.',
    src2: '[Avoiding fallback in distributed systems](https://aws.amazon.com/builders-library/avoiding-fallback-in-distributed-systems/) — Jacob Gabrielson, AWS. A indisponibilidade de 2001 e o argumento contra fallbacks.',
    src3: '[Addressing cascading failures](https://sre.google/sre-book/addressing-cascading-failures/) — livro de SRE do Google. O exemplo das 5.000 threads.',
    src4: '[Circuit breakers](https://brooker.co.za/blog/2022/02/16/circuit-breakers.html) — o argumento de Marc Brooker contra eles.',
    src5: '[Documentação do Resilience4j para Spring Boot](https://resilience4j.readme.io/docs/getting-started-3) — a ordem dos decorators acima.',
    src6: 'Michael Nygard, *Release It!* — a origem dos padrões Timeout e Bulkhead. “Pontos de integração são os assassinos número um de sistemas.”',
    footer: 'O código por trás de cada figura desta página está em [arthurd3/time-limiter-resillience4j](https://github.com/arthurd3/time-limiter-resillience4j). As simulações são funções puras em `frontend/src/sim/`, testadas contra o comportamento descrito aqui.',
  },
}

export const COPY: Record<Locale, Copy> = { en, pt }
